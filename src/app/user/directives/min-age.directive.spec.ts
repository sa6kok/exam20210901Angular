import { MinAgeDirective } from './min-age.directive';

describe('MinAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new MinAgeDirective();
    expect(directive).toBeTruthy();
  });
});
