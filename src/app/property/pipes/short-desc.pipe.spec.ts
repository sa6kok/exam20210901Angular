import { ShortDescPipe } from './short-desc.pipe';

describe('ShortDescPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortDescPipe();
    expect(pipe).toBeTruthy();
  });
});
