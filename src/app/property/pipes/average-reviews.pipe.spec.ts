import { AverageReviewsPipe } from './average-reviews.pipe';

describe('AverageReviewsPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageReviewsPipe();
    expect(pipe).toBeTruthy();
  });
});
