import { ShowCorectNumberPipe } from './show-corect-number.pipe';

describe('ShowCorectNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCorectNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
