import { DateShowDbPipe } from './date-show-db.pipe';

describe('DateShowDbPipe', () => {
  it('create an instance', () => {
    const pipe = new DateShowDbPipe();
    expect(pipe).toBeTruthy();
  });
});
