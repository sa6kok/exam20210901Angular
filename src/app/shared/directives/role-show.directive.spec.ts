import { RoleShowDirective } from './role-show.directive';

describe('RoleShowDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleShowDirective();
    expect(directive).toBeTruthy();
  });
});
